{
  "name": "alpha-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/alpha-app/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome check packages/alpha-app/src"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome format --write packages/alpha-app/src"
      }
    },
    "format:check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome format packages/alpha-app/src"
      }
    }
  }
}
